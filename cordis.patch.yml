# dsh-worldbook — bundle patch 层（供 `dsh.profile.bundles` 自动挂载）。
#
# 本文件由 package.json 的 `dsh.bundle.patch` 声明引用：把本包加入 profile
# 的 bundle 层后，DSH 启动时自动应用本 patch，无需 --patch 手动挂载。
- insert:
    - id: worldbook
      name: 'dsh-worldbook'
